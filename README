RjzServer, by Chris McCormick <chris@mccormick.cx>

GPLv3 licensed. See the file COPYING for details.

This is not a very secure server, so don't run it on public facing networks. Intended for internal LAN use only.

Dependencies:
wxPython library

Build:
run build-mac.sh for Mac OSX
run build-windows.py for Windows
Linux users run `python rjzserver.py` :)
